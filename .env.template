# Docker Compose — só API (Postgres externo; credenciais vêm do .env)
# HTTP_PORT_PUBLISH=8080
# Com Postgres na máquina host (Beekeeper): DB_HOST_LOCAL=host.docker.internal
# RUN_MIGRATIONS_ON_START=true   # opcional: migrate ao subir o container

ENVIRONMENT=

DB_HOST_LOCAL=
DB_PORT_LOCAL=
DB_USER_LOCAL=
DB_PASSWORD_LOCAL=

DB_NAME_LOCAL=
DB_HOST_PROD=
DB_PORT_PROD=
DB_USER_PROD=
DB_PASSWORD_PROD=
DB_NAME_PROD=

COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_REGION=

PORT=

RUN_MIGRATIONS_ON_START=

# AWS — credenciais do usuário IAM (Opção B)
AWS_ACCESS_KEY_ID=AKIA
AWS_SECRET_ACCESS_KEY=
AWS_REGION=